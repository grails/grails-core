<project name="grails-cruisecontrol-starter" default="cruise">

	<target name="cruise">
	    <echo> starting in cruise build in ${basedir}</echo>
	    <mkdir dir="../build/log"/>
	    <ant dir=".." antfile="build.xml" />
	</target>
			
</project>