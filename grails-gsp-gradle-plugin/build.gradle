apply plugin: 'groovy'
apply plugin: 'maven'

group = 'org.grails'

repositories {
  maven { url "http://repo.spring.io/libs-release" }
  mavenLocal()
  mavenCentral()

}

dependencies {
    compile gradleApi()
}
